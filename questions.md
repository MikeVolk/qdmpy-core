- why is validate_frequencies in constants.py?
- do we need a new project: QDMpy Server with a webinterface for quick fitting? MAybe a monitor for certain folders?
- maybe we want a metadata.md/toml,yaml that is read with the other data by measuremnet.load?
- can we detect the optimal fluorescence correction by taking a strong pixel (large deviation of mean), then fit both a mean and a model to it at the same time?
- odmr.plotspectra should take a list of either [(x,y)] or [x], [y], what about: [x1,x2,x3], y for a line of pixels?
- should plot_spectra in odmr go into the plotting module? Does it need to be in the class?
- MagneticMap.display seems to not work I get some plt.show error
- there is a /site, /docs, /tutorials, /scripts, /notebooks, /examples that seems very messy
- we need to check the skipped tests
- we measure a high and a low freq range so that the central spectra, which we are not interested in is blocked. i noticed that sometimes the inner spectrum (which is usually not measured) shifts into the range we do measure, so i would like to be able to set lower and upper cutoff ranges to the fits so the fit is only done inside the frange specified. by default it should be the full range. this should be setable for both high and log frange separately and apply to both polarities ( separating this for polarities may come in the future)